#include <stdio.h>

/*
 This is comment section to describe the code 
 1. Define an interger variable named month_count with value 12
 2. if month_count is more than 6
 3. we display the message 
 */

int main(){
	
	int month_count = 12;

	if (month_count > 6){
		puts("Code everyday,To become a good programmer.");   // why puts() instead of printf() | just search online ask to the LLM.

	}
	
	return 0;
}
